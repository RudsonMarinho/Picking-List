services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: picking_list
    ports:
      - "5432:5432"
    volumes:
      - picking_list_data:/var/lib/postgresql/data

volumes:
  picking_list_data:
